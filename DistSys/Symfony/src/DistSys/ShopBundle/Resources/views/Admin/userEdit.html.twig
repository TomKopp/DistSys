
<h1>Nutzerdaten</h1>
<form class="form-horizontal" id="profile-form" action="{{ path('admin_user_update', {'id': user.id}) }}" method="post" {{ form_enctype(profileform) }}>
      {% if error is defined %} 
    {{ error }}
  {% endif %}
  <div class="form-group">
  {{ form_label(profileform.username) }}
  {{ form_errors(profileform.username) }}
	  <div class="col-lg-5">
	    {{ form_widget(profileform.username) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(profileform.email) }}
  {{ form_errors(profileform.email) }}
	  <div class="col-lg-5">
	  {{ form_widget(profileform.email) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(profileform.firstname) }}
  {{ form_errors(profileform.firstname) }}
	  <div class="col-lg-5">
	  {{ form_widget(profileform.firstname) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(profileform.lastname) }}
  {{ form_errors(profileform.lastname) }}
	  <div class="col-lg-5">
	  {{ form_widget(profileform.lastname) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(profileform.male) }}
  {{ form_errors(profileform.male) }}
	  <div class="col-lg-5">
	  {{ form_widget(profileform.male) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(profileform.street) }}
  {{ form_errors(profileform.street) }}
	  <div class="col-lg-5">
	  {{ form_widget(profileform.street) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(profileform.postal) }}
  {{ form_errors(profileform.postal) }}
	  <div class="col-lg-5">
	  {{ form_widget(profileform.postal) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(profileform.city) }}
  {{ form_errors(profileform.city) }}
	  <div class="col-lg-5">
	  {{ form_widget(profileform.city) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(profileform.phone) }}
  {{ form_errors(profileform.phone) }}
	  <div class="col-lg-5">
	  {{ form_widget(profileform.phone) }}
	  </div>
  </div>
    <p>
        <button class="btn btn-primary" type="submit">speichern</button>
    </p>
    <p class="res">
    </p>
    {{ form_end(profileform) }}
    <p>&nbsp;
    </p>
    <p>&nbsp;
    </p>
    <p>&nbsp;
    </p>
</form>
