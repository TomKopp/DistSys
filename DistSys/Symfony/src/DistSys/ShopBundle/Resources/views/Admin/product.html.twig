{% extends 'DistSysShopBundle::Scaffolding/scaffolding.html.twig' %}

{% block main %}


<h2 class="heading style1">Produkte Verwalten</h2>
                        <table class="table table-condensed" width="500px" cellpadding="0" cellspacing="0">
                          <thead>
                            <tr>
                              <th>Product</th>
                              {#  <th width="100">Bestellungen</th> #}
                              <th >Anzeigen</th>
                              <th>Bearbeiten</th>
                              <th >Löschen</th>
                            </tr>
                          </thead>
                          <tbody>
                            {% for product in products %}
                            <tr>
                              <td><p>{{ product.name }}</p></td>
                              {#<td><a href="">Bestellungen</a></td>#}
                              <td><a class="prodShow btn btn-success"  href="{{ path('admin_product_show', {'id': product.id}) }}">Show</a></td>
                              <td><a class="prodEdit btn btn-primary"  href="{{ path('admin_product_edit', {'id': product.id}) }}">Edit</a></td>
                              <td><a class="prodDelete btn btn-danger"  href="{{ path('admin_product_remove', {'id': product.id}) }}">Remove</a></td>

                            </tr>
                            {% endfor %}
                          </tbody>
                          <tfoot>
                            <tr>
                              <td colspan="5"></td>
                            </tr>
                          </tfoot>
                        </table>
                        
      <a class="prodNew btn btn-default"  href="{{ path('admin_product_new') }}">Neues Produkt</a>
    <div id="render">
    
     </div>
    


{% endblock %}