
<h1>Neues Produkt anlegen</h1>
<form class="form-horizontal" id="prod-form" action="{{ path('admin_product_save') }}" method="post" {{ form_enctype(prodForm) }}>
      {% if error is defined %} 
    {{ error }}
  {% endif %}
  <div class="form-group">
  {{ form_label(prodForm.product.name) }}
  {{ form_errors(prodForm.product.name) }}
	  <div class="col-lg-5">
	    {{ form_widget(prodForm.product.name) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(prodForm.product.description) }}
  {{ form_errors(prodForm.product.description) }}
	  <div class="col-lg-5">
	    {{ form_widget(prodForm.product.description) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(prodForm.product.price) }}
  {{ form_errors(prodForm.product.price) }}
	  <div class="col-lg-5">
	    {{ form_widget(prodForm.product.price) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(prodForm.product.stock) }}
  {{ form_errors(prodForm.product.stock) }}
	  <div class="col-lg-5">
	    {{ form_widget(prodForm.product.stock) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(prodForm.product.status) }}
  {{ form_errors(prodForm.product.status) }}
	  <div class="col-lg-5">
	    {{ form_widget(prodForm.product.status) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(prodForm.product.attributes) }}
  {{ form_errors(prodForm.product.attributes) }}
	  <div class="col-lg-5">
	    {{ form_widget(prodForm.product.attributes) }}
	  </div>
  </div>
  <div class="form-group">
  {{ form_label(prodForm.product.galleryItems) }}
  {{ form_errors(prodForm.product.galleryItems) }}
	  <div class="col-lg-5">
	    {{ form_widget(prodForm.product.galleryItems) }}
	  </div>
  </div>
  

    <p>
        <button class="btn btn-primary" type="submit">speichern</button>
    </p>
    <p class="res">
    </p>
    {{ form_end(prodForm) }}
    <p>&nbsp;
    </p>
    <p>&nbsp;
    </p>
    <p>&nbsp;
    </p>
</form>
